<Application x:Class="CreateBatchFilesForXbox360XBLAGames.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml"
             ShutdownMode="OnMainWindowClose">
    <Application.Resources>

        <!-- ==================== COLOR PALETTE ==================== -->
        <!--
            Harmonious Dark Theme with Green Terminal Accent
            Primary: Deep Slate (#1a1d21)
            Secondary: Teal/Cyan (#00bcd4) - complements the green terminal
            Accent: Soft Green (#4caf50) - harmonizes with terminal green
            Terminal: Black (#0a0a0a) + Neon Green (#39FF14) - PRESERVED
        -->

        <!-- ==================== TERMINAL COLORS (PRESERVED) ==================== -->
        <SolidColorBrush x:Key="TerminalBackgroundBrush" Color="#0a0a0a"/>
        <SolidColorBrush x:Key="TerminalBorderBrush" Color="#1a1a1a"/>
        <SolidColorBrush x:Key="TerminalForegroundBrush" Color="#39FF14"/>
        <SolidColorBrush x:Key="TerminalSelectedBrush" Color="#2a2a2a"/>

        <!-- ==================== WINDOW & BACKGROUND COLORS ==================== -->
        <!-- Deep Slate Theme Base -->
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#121518"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#1e2227"/>
        <SolidColorBrush x:Key="CardBackgroundHoverBrush" Color="#252a30"/>
        <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="#0d0f12"/>

        <!-- ==================== BORDER COLORS ==================== -->
        <SolidColorBrush x:Key="BorderBrush" Color="#2d333b"/>
        <SolidColorBrush x:Key="BorderBrushLight" Color="#3d454f"/>

        <!-- ==================== TEXT COLORS ==================== -->
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#e6edf3"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#8b949e"/>
        <SolidColorBrush x:Key="TextMutedBrush" Color="#6e7681"/>
        <SolidColorBrush x:Key="HeaderTextBrush" Color="#ffffff"/>

        <!-- Status Text Colors - Harmonious with theme -->
        <SolidColorBrush x:Key="SuccessTextBrush" Color="#4caf50"/>
        <SolidColorBrush x:Key="FailedTextBrush" Color="#f85149"/>
        <SolidColorBrush x:Key="SkippedTextBrush" Color="#ffa726"/>
        <SolidColorBrush x:Key="LinkTextBrush" Color="#58a6ff"/>
        <SolidColorBrush x:Key="InfoTextBrush" Color="#00bcd4"/>

        <!-- ==================== PRIMARY ACCENT COLORS ==================== -->
        <!-- Teal/Cyan as primary - complements the green terminal -->
        <SolidColorBrush x:Key="PrimaryAccentBrush" Color="#00bcd4"/>
        <SolidColorBrush x:Key="PrimaryAccentHoverBrush" Color="#00e5ff"/>
        <SolidColorBrush x:Key="PrimaryAccentDarkBrush" Color="#008ba3"/>

        <!-- Secondary accent - Soft Green (harmonizes with terminal) -->
        <SolidColorBrush x:Key="SecondaryAccentBrush" Color="#4caf50"/>
        <SolidColorBrush x:Key="SecondaryAccentHoverBrush" Color="#66bb6a"/>
        <SolidColorBrush x:Key="SecondaryAccentDarkBrush" Color="#388e3c"/>

        <!-- ==================== BUTTON COLORS ==================== -->
        <!-- Action Button (Teal) -->
        <SolidColorBrush x:Key="ActionButtonBrush" Color="#00bcd4"/>
        <SolidColorBrush x:Key="ActionButtonHoverBrush" Color="#00e5ff"/>
        <SolidColorBrush x:Key="ActionButtonPressedBrush" Color="#008ba3"/>

        <!-- Start/Success Button (Green - harmonizes with terminal) -->
        <SolidColorBrush x:Key="StartButtonBrush" Color="#4caf50"/>
        <SolidColorBrush x:Key="StartButtonHoverBrush" Color="#66bb6a"/>
        <SolidColorBrush x:Key="StartButtonPressedBrush" Color="#388e3c"/>

        <!-- Cancel/Error Button (Soft Red) -->
        <SolidColorBrush x:Key="CancelButtonBrush" Color="#f85149"/>
        <SolidColorBrush x:Key="CancelButtonHoverBrush" Color="#ff6b6b"/>
        <SolidColorBrush x:Key="CancelButtonPressedBrush" Color="#da3633"/>

        <!-- About/Info Button (Purple - complementary to teal) -->
        <SolidColorBrush x:Key="AboutButtonBrush" Color="#7c4dff"/>
        <SolidColorBrush x:Key="AboutButtonHoverBrush" Color="#9e7bff"/>
        <SolidColorBrush x:Key="AboutButtonPressedBrush" Color="#5e35b1"/>

        <!-- Exit Button (Slate Gray) -->
        <SolidColorBrush x:Key="ExitButtonBrush" Color="#607d8b"/>
        <SolidColorBrush x:Key="ExitButtonHoverBrush" Color="#78909c"/>
        <SolidColorBrush x:Key="ExitButtonPressedBrush" Color="#455a64"/>

        <!-- Disabled State -->
        <SolidColorBrush x:Key="DisabledButtonBrush" Color="#3d454f"/>
        <SolidColorBrush x:Key="DisabledTextBrush" Color="#6e7681"/>

        <!-- ==================== NAVIGATION COLORS ==================== -->
        <SolidColorBrush x:Key="NavigationUnderlineBrush" Color="#00bcd4"/>
        <SolidColorBrush x:Key="NavigationHoverBrush" Color="#1e2227"/>

        <!-- ==================== STATUS BAR ==================== -->
        <SolidColorBrush x:Key="StatusBarBackgroundBrush" Color="#0d0f12"/>

        <!-- ==================== DEFAULT STYLE TARGET TYPES ==================== -->

        <!-- Default ToolTip Style -->
        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Placement" Value="MousePoint"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              Content="{TemplateBinding Content}"
                                              ContentTemplate="{TemplateBinding ContentTemplate}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Default Label Style -->
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Padding" Value="5"/>
        </Style>

        <!-- Default TextBlock Style -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Default TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="MinHeight" Value="30"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="CaretBrush" Value="{StaticResource PrimaryAccentBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="Border"
                                 Background="{TemplateBinding Background}"
                                 BorderBrush="{TemplateBinding BorderBrush}"
                                 BorderThickness="{TemplateBinding BorderThickness}"
                                 CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                           Margin="{TemplateBinding Padding}"
                                           Focusable="False"
                                           HorizontalScrollBarVisibility="Hidden"
                                           VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource PrimaryAccentBrush}"/>
                                <Setter TargetName="Border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Default StatusBar Style -->
        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="{StaticResource StatusBarBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Default StatusBarItem Style -->
        <Style TargetType="StatusBarItem">
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>

        <!-- Default Border Style for Cards/Panels -->
        <Style TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="4"/>
        </Style>

        <!-- Card Border Style (for sections) -->
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <!-- ==================== NAMED STYLES ==================== -->

        <!-- Primary Action Button Style -->
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="30"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#121518"/>
            <Setter Property="Background" Value="{StaticResource ActionButtonBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                 Background="{TemplateBinding Background}"
                                 CornerRadius="6"
                                 BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource DisabledButtonBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource DisabledTextBrush}"/>
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource ActionButtonHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource ActionButtonPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Start Button Style -->
        <Style x:Key="StartButtonStyle" BasedOn="{StaticResource ActionButtonStyle}" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource StartButtonBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                 Background="{TemplateBinding Background}"
                                 CornerRadius="6"
                                 BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource DisabledButtonBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource DisabledTextBrush}"/>
                                <Setter Property="Opacity" Value="0.7"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource StartButtonHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="{StaticResource StartButtonPressedBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ==================== LOG TEXTBOX STYLES ==================== -->
        <!-- Terminal TextBox Style (reused across windows) -->
        <Style x:Key="TerminalLogTextBox" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="{StaticResource TerminalBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TerminalForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="AcceptsReturn" Value="True"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="TextAlignment" Value="Left"/>
            <Setter Property="TextWrapping" Value="NoWrap"/>
            <Setter Property="CaretBrush" Value="{StaticResource TerminalForegroundBrush}"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
        </Style>

        <!-- Terminal TextBox with Glow Effect -->
        <Style x:Key="TerminalLogTextBoxWithGlow" TargetType="TextBox" BasedOn="{StaticResource TerminalLogTextBox}">
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="0" Color="#39FF14" BlurRadius="2" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ==================== BUTTON CONTENT COLORS ==================== -->
        <!-- Dark text color for buttons with light backgrounds -->
        <SolidColorBrush x:Key="ButtonTextDarkBrush" Color="#121518"/>

        <!-- ==================== APPLICATION-SPECIFIC NAMED STYLES ==================== -->

        <!-- Header Title TextBlock (main window title) -->
        <Style x:Key="HeaderTitleStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="{StaticResource HeaderTextBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- About Window Title -->
        <Style x:Key="AboutTitleStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Foreground" Value="{StaticResource HeaderTextBrush}"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <!-- About Window Subtitle (version) -->
        <Style x:Key="AboutSubtitleStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>

        <!-- About Window Section Header -->
        <Style x:Key="AboutSectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>

        <!-- About Window Body Text -->
        <Style x:Key="AboutBodyTextStyle" TargetType="TextBlock">
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
        </Style>

        <!-- Button Content TextBlock (dark text on light buttons) -->
        <Style x:Key="ButtonContentTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource ButtonTextDarkBrush}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

    </Application.Resources>
</Application>
